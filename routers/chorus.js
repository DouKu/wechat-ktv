import nconf from 'nconf'

module.exports = (router, commonRouter, authRouter, wechatRouter) => {
	// commonRouter.get('/chrous', getAllXinMusic)
	// commonRouter.post('/chrous', addXinMusic)
	// commonRouter.put('/chrous', updateXinMusic)
}